---
title: "Data Science Project"
date: "2025-11-15"
---

Skup podataka pod nazivom *personality_synthetic_dataset.csv* sadrži informacije koje, na osnovu različitih uzetih parametara, omogućavaju uvid u tip ličnosti ispitanika. Nas cilj je da ispitamo povezanost između odabranih parametara i tipa ličnosti, kako bismo napravili što precizniji model koji može predvideti tip ličnosti ispitanika.

Ciljna promenljiva, ciju vrednost je model potrebno predvideti na osnovu zavisnih parametara je *personality_type*.

Za potrebe analize podataka koristicemo programski jezik *R* i biblioteke koje on nudi.

Najpre ćemo učitati skup podataka koji je u *.csv* formata.

```{r}

ds = read.csv("personality_synthetic_dataset.csv", stringsAsFactors = FALSE)

```

Nakon učitavanja skupa podataka, možemo pogledati njegove dimenzije. 
Vidimo da ovaj skup podataka ima 20 000 redova i 30 kolona što predstavlja dovoljnu količinu podataka za pravljenje modela i njegovo testiranje.

```{r}

dim(ds)

```

Pomoću komande *str* možemo pogledati strukturu promenljivih unutar skupa podataka. Možemo primetiti da je promenljiva *personality_type* jedina kategorijska u skupu podataka, a to je ujedno i ciljna promenljiva. 
Ostale promenljive su kategorijske numericke vrednosti.
```{r}
str(ds)
```

Analizom minimalnih i maksimalnih vrednosti numerickih vrednosti, mozemo primetiti da su sve unutar intervala [0, 10].
Dodatno, primecujemo da nema nedostajucih vrednosti. 
```{r}

summary(ds)

```
Mozemo proveriti za *personality_type* da li ima prazan string kao vrednost tj. *""* sto je isto nedostajuca vrednost, ali u drugom formatu. To cemo proveriti sledecom komandom:
```{r}
table(ds['personality_type'])
```
Vidimo da nemamo *""* kao jednu od kategorija i sada sa sigurnoscu mozemo reci da ovaj skup podataka nema nedostajuce vrednosti.

-------------------------------------------------------------
Mozemo preci na analizu pojedinacnih promenljivih unutar skupa podataka kako bismo se dodatno upoznali sa njim.

Krenimo od ciljne promenljive *personality_type* koja predstavlja tip licnosti.
Ovo je ujedno i jedina kategorijska promenljiva u našem skupu podataka koja je podeljena u 3 kategorije:
1.	Introvert – osoba koja uživa u mirnim i samostalnim aktivnostima 
2.	Extrovert – osoba koja uživa da bude u društu, voli interakciju sa ljudima i društvene aktivnosti
3.	Ambivert – osoba koja kombinuje oba prethodna tipa tako što se prilagođava situaciji

Pokazimo procentualnu zastupljenost svake kategorije. 
```{r}
prop.table(table(ds['personality_type'])) * 100
```
Mozemo primetiti da je zastupljenost sva tri tipa licnosti ravnomerna sto je odlicno za dalju obradu seta podataka. 
Prikazimo ovo i vizuelno pomocu grafika:

```{r}
ggplot(data = ds, aes(x = personality_type, fill = personality_type)) + 
  geom_bar() + 
  labs(title = "Zastupljenost razlicitih tipova licnosti u skupu podataka",
         x = "Tip licnosti",
         y = "Broj pojavljivanja",
         fill = "Tip licnosti") + 
  theme_minimal()

```

*social_energy* je promenljiva koja predstavlja u kojoj meri osoba crpi energiju iz drustvenih interakcija, gde 0 označava da osoba ne uziva u drustvu, a 10 da osoba veoma uziva u drustvu. 
Veca vrednost oznacava veću sklonost ka interakciji sa drugima.

Na ovom grafiku mozemo primetiti tendenciju ka normalnoj raspodeli, ali mozemo nacrtati i histogram, kako bismo videli po intervalima koliko ispitanika ima odredjene vrednosti.

```{r}
ggplot(data = ds, aes(x = social_energy)) + 
  geom_density(fill = "tomato", alpha = 0.6) + 
  labs(title = "Graficki prikaz raspodele promenljive 'social_energy'",
       y = 'Procenat zastupljenosti')
  theme_minimal()
```



```{r}
ggplot(ds, aes(x = social_energy)) +
  geom_histogram(binwidth = 1, fill = "tomato", color = "black") +
  labs(title = "Graficki prikaz zastupljenosti intervala promenljive 'social_energy'",
       y = "Broj ispitanika") +
  theme_minimal()

```


*alone_time_preference* je promenljiva koja predstavlja koliko osoba uziva i voli da provodi vreme sama, pri cemu visa vrednost predstavlja vece uzivanje u tome. 
Na osnovu grafika ispod, mozemo zakljuciti da i ova promenljiva ima tendenciju ka normalnoj raspodeli. 

```{r}
ggplot(data = ds, aes(x = alone_time_preference)) + 
  geom_density(fill = "tomato", alpha = 0.6) + 
  labs(title = "Graficki prikaz raspodele promenljive 'alone_time_preference'",
       y = 'Procenat zastupljenosti')
  theme_minimal()
```



```{r}
ggplot(ds, aes(x = alone_time_preference)) +
  geom_histogram(binwidth = 1, fill = "tomato", color = "black") +
  labs(title = "Graficki prikaz zastupljenosti intervala promenljive 'alone_time_preference'",
       y = "Broj ispitanika") +
  theme_minimal()
```

Odnos ciljane promenljive (personality_type) sa ostalim promenljivama u skupu
Primenom ANOVA test proveravamo da li se prosecne vrednosti promenljivih razlikuju izmedju tri tipa licnosti (Introvert, Ambivert, Extrovert)

```{r}
anova_socialEnergy <- aov(social_energy ~ personality_type, data = ds)
summary(anova_socialEnergy)

```
Rezultati testa pokazuju da kod promenljive social_energy postoji statisticki znacajna razlika izmedju tipova licnosti(p<0.05)

```{r}
ggplot(ds,aes(x=personality_type,y=social_energy,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost socijalne energije od tipa licnosti",
       x="Tip licnosti",
       y="Socijalna energija")
```
Na osnovu boxplot dijagrama uocavamo da:
  -Extroverti imaju najvise vrednosti socijalne energije
  -Introverti najmanje
  -Ambiverti se nalaze izmedju ova dva tipa licnosti
  
```{r}
anova_aloneTimePref <- aov(alone_time_preference ~ personality_type, data = ds)
summary(anova_aloneTimePref)
```
Rezultati pokazuju da se tri tipa licnosti medjusobno razlikuju u stepenu preferiranja vremena provedenog u samoci (p<0.05)
```{r}
ggplot(ds,aes(x=personality_type,y=alone_time_preference,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost alone_time_preference od tipa licnosti",
       x="Tip licnosti",
       y="Alone time preference")
```

Sa dijagrama mozemo zakljuciti da:
  -Introverti najvise preferiraju vreme provedeno sami
  -Ekstroverti najmanje
  -Ambiverti se nalaze izmedju ova dva tipa licnosti
Ocekivano je da ekstroverti imaju najnize vrednosti, sto je u skladu sa njihovom tendencijom ka cescoj socijalnoj interakciji.

```{r}
anova_talkativeness <- aov(talkativeness ~ personality_type, data = ds)
summary(anova_talkativeness)
```

Rezultati testa pokazuju da kod promenljive talkativeness postoji statisticki znacajna razlika izmedju tipova licnosti(p<0.05)

```{r}
ggplot(ds,aes(x=personality_type,y=talkativeness,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost pricljivosti od tipa licnosti",
       x="Tip licnosti",
       y="Pricljivost")
```

Sa dijagrama mozemo zakljuciti da:
  -Ekstrovertne osobe najvise vole da komuniciraju
  -Introvertne najmanje
  -Ambivertne su izmedju ova tipa licnosti

```{r}
anova_deepReflection <- aov(deep_reflection ~ personality_type, data = ds)
summary(anova_deepReflection)
```
Rezultati pokazuju da se tri tipa licnosti medjusobno razlikuju u stepenu ucestalosti dubokog razmisljanja (p<0.05)

```{r}
ggplot(ds,aes(x=personality_type,y=deep_reflection,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost ucestalosti dubokog razmisljanja od tipa licnosti",
       x="Tip licnosti",
       y="Ucestalost dubokog razmisljanja")
```
Sa dijagrama mozemo zakljuciti da:
  -Introverti imaju sklonost ka dubokoj analizi o sebi, iskustvima, dogadjajima...
  -Ekstroverti retko ulaze u dublja razmisljanja
  -Ambiverti su izmedju
  
```{r}
anova_groupComfort <- aov(group_comfort ~ personality_type, data = ds)
summary(anova_groupComfort)
```
Rezultati pokazuju da se tri tipa licnosti medjusobno razlikuju u stepenu prijatnosti u grupi (p<0.05)


```{r}
ggplot(ds,aes(x=personality_type,y=group_comfort,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost osecaja prijatnosti u grupi od tipa licnosti",
       x="Tip licnosti",
       y="Prijatnost u grupi")
```

Sa dijagrama mozemo zakljuciti da:
  -Ekstroverti se najprijatnije osecaju u grupi
  -Introverti najmanje
  -Ambiverti su izmedju
  
```{r}
anova_partyLiking <- aov(party_liking ~ personality_type, data = ds)
summary(anova_partyLiking)
```

Rezultati pokazuju da se tri tipa licnosti medjusobno razlikuju u meri uzivanja u zurkama i drustvenim dogadjajima (p<0.05)

```{r}
ggplot(ds,aes(x=personality_type,y=party_liking,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost sklonosti zabavama od tipa licnosti",
       x="Tip licnosti",
       y="Sklonost ka zabavama")
```
Sa dijagrama zakljucujemo da:
  -Ekstroverti najvise uzivaju u zurkama i drustvenim dogadjajima
  -Introverti najmanje uzivaju
  -Ambiverti su izmedju ova 2 tipa licnosti
  
```{r}
anova_Empathy <- aov(empathy ~ personality_type, data = ds)
summary(anova_Empathy)
```
Rezultati testa pokazuju da se tri tipa licnosti medjusobno razlikuju u meri sposobnosti razumevanja osecanja drugih (p<0.05)

```{r}
ggplot(ds,aes(x=personality_type,y=empathy,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost empatije od tipa licnosti",
       x="Tip licnosti",
       y="Empatija")
```

Sa dijagrama mozemo zakljuciti da:
    -Introverti najvise razumeju osecanja drugih
    -Ekstroverti najmanje
    -Ambiverti su izmedju 
Mozemo primetiti da razlika ovde nije drasticna.

```{r}
anova_creativity <- aov(creativity ~ personality_type, data = ds)
summary(anova_creativity)
```
Rezultati testa nam pokazuju da ne postoji statisticki znacajna razlika u kreativnosti izmedju tri tipa licnosti

```{r}
anova_organization <- aov(organization ~ personality_type, data = ds)
summary(anova_organization)
```

Rezultati testa pokazuju da se tri tipa licnosti medjusobno razlikuju u stepenu sklonosti ka organizovanosti, redu i planiranjuobaveza.(p<0.05)

```{r}
ggplot(ds,aes(x=personality_type,y=organization,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost organizovanosti od tipa licnosti",
       x="Tip licnosti",
       y="Organizovanost")
```

Sa grafika mozemo zaljkuciti da:
  -Introverti su najorganizovaniji
  -Ekstroverti najmanje organizovani
  -Ambiverti su izmedju introverta i ekstroverta

```{r}
anova_leadership <- aov(leadership ~ personality_type, data = ds)
summary(anova_leadership)
```
Rezultati pokazuju da se tri tipa licnosti medjusobno razlikuju u meri osecaja prijatnosti i samouverenosti u ulozi lidera (p<0.05)

```{r}
ggplot(ds,aes(x=personality_type,y=leadership,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost osecaja prijatnosti i samouverenosti u ulozi lidera od tipa licnosti",
       x="Tip licnosti",
       y="Osecaj prijatnosti i samouverenosti u ulozi lidera")
```
Sa dijagrama mozemo zakljuciti da:
  -Ekstrovertne osobe se osecaju najprijatnije u ulozi lidera
  -Introvertne osobe se osecaju najmanje prijatno u ulozi lidera
  -Ambivertne osobe su izmedju ova dva tipa licnosti
  
```{r}
anova_riskTaking <- aov(risk_taking ~ personality_type, data = ds)
summary(anova_riskTaking)
```

Rezultati testa pokazuju da se tri tipa licnosti medjusobno razlikuju u stepenu spremnosti preuzimanja rizika (p<0.05)

```{r}
ggplot(ds,aes(x=personality_type,y=risk_taking,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost sklonosti riziku od tipa licnosti",
       x="Tip licnosti",
       y="Spremnost na rizik")
```

Sa dijagrama zakljucujemo da:
  -Ekstroverti su najvise spremni da preuzmu rizik
  -Introverti su najmanje spremni za preuzimanje rizika
  -Ambiverti su izmedju ova dva tipa
  
```{r}
anova_publicSpeakingComfort <- aov(public_speaking_comfort ~ personality_type, data = ds)
summary(anova_publicSpeakingComfort)
```
Rezultati pokazuju da se tri tipa licnosti medjusobno razlikuju u stepenu osecaja prijatnosti prilikom javnog nastupa (p<0.05)

```{r}
ggplot(ds,aes(x=personality_type,y=public_speaking_comfort,fill=personality_type)) + 
  geom_boxplot() + 
  theme_bw() + 
  labs(title = "Zavisnost prijatnosti prilikom javnog nasupa od tipa licnosti",
       x="Tip licnosti",
       y="Osecaj prijatnosti prilikom javnog nastupa")
```

Sa dijagrama mozemo zakljuciti da:
  -Ekstrovertne osobe se najprijatnije osecaju prilikom javnog nastupa
  -Introvertne osobe se najmanje prijatno osecaju prilikom javnog nastupa
  -Ambivertne osobe su izmedju ova dva tipa licnosti
